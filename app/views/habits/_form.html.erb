<%= form_for(@habit) do |f| %>
  <% if @habit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@habit.errors.count, "error") %> prohibited this habit from being saved:</h2>

      <ul>
      <% @habit.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :aftertrigger %><br>
    <%= f.text_area :aftertrigger %>
  </div>
  <div class="field">
    <%= f.label :behavior %><br>
    <%= f.text_area :behavior %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
